<Window x:Class="SimpleCalculatorMVVM.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SimpleCalculatorMVVM.ViewModels"
        Title="Калькулятор MVVM" Height="650" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="Segoe UI"
        Background="#F0F0F0">

    <Window.DataContext>
        <vm:CalculatorViewModel/>
    </Window.DataContext>

    <Grid x:Name="MainGrid" Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Меню выбора режима -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Center"
                    Margin="0,0,0,10">
            <TextBlock Text="Режим:" 
                       VerticalAlignment="Center" 
                       Margin="0,0,8,0" 
                       FontSize="13"
                       Foreground="#333333"/>
            <ComboBox x:Name="ModeSelector" 
                      Width="110" 
                      Height="28"
                      SelectedIndex="0"
                      FontSize="13"
                      BorderBrush="#CCCCCC"
                      Background="White">
                <ComboBoxItem Content="Стандартный"/>
                <ComboBoxItem Content="Научный"/>
            </ComboBox>
        </StackPanel>

        <!-- Дисплей -->
        <Border Grid.Row="1" 
                Background="#F5F5F5" 
                CornerRadius="8" 
                Margin="0,0,0,20"
                BorderBrush="#DDDDDD"
                BorderThickness="1">
            <TextBox Text="{Binding Display, UpdateSourceTrigger=PropertyChanged}" 
                     Height="80" 
                     Background="Transparent"
                     BorderThickness="0"
                     FontSize="36" 
                     FontWeight="Light"
                     TextAlignment="Right" 
                     VerticalContentAlignment="Center"
                     Padding="15"
                     IsReadOnly="True"/>
        </Border>



        <!-- Сетка для кнопок -->
        <Grid x:Name="ButtonsGrid" Grid.Row="3" Margin="0,0,0,39" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        </Grid>
    </Grid>
</Window>